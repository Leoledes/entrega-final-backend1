<h1>Lista de Productos</h1>

{{#if products.length}}
<div class="products-container">
  {{#each products}}
    <div class="product-card">
      <h3>{{this.name}}</h3>
      
      <p class="price-tag">${{this.price}} <span>(Stock: {{this.stock}})</span></p>
      <p><strong>Categoría:</strong> {{this.category}}</p>
      <p><strong>Descripción:</strong> {{this.description}}</p>

      <div class="actions-container">
        <a href="/products/{{this.id}}" class="btn-detail">Ver Detalle</a>
        <button onclick="addToCart('{{../cartId}}', '{{this.id}}')">🛒 Agregar</button>
      </div>
    </div>
  {{/each}}
</div>

<div class="pagination">
  {{#if hasPrevPage}}
    <a href="{{prevLink}}">⬅️ Anterior</a>
  {{/if}}

  <span>Página {{page}} de {{totalPages}}</span>

  {{#if hasNextPage}}
    <a href="{{nextLink}}">Siguiente ➡️</a>
  {{/if}}
</div>
{{else}}
<p class="no-products">No hay productos cargados.</p>
{{/if}}

<script src="/js/cart.js"></script>
