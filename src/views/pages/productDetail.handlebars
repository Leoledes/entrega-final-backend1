<div class="product-detail-card"> 
    <h1>{{product.name}}</h1>

    <p class="price-tag">${{product.price}} <span>(Stock: {{product.stock}})</span></p>

    <p><strong>Categoría:</strong> {{product.category}}</p>
    <p><strong>Descripción:</strong> {{product.description}}</p>

    <div class="actions-container">
        <button onclick="addToCart('{{cartId}}', '{{product.id}}')">🛒 Agregar al Carrito</button>
        <a href="/products" class="btn-detail">⬅️ Volver a Productos</a>
    </div>
</div>

<script src="/js/cart.js"></script>
